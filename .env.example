# Default is the deterministic, offline backend — no key needed.
LLM_PROVIDER=rule_based

# To use a real LLM, install the extra and switch provider:
#   pip install langchain-openai
# LLM_PROVIDER=openai
# LLM_MODEL=gpt-4o-mini
# OPENAI_API_KEY=sk-...
